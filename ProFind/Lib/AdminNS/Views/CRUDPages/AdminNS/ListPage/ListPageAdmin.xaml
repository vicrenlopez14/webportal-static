<Page
    x:Class="ProFind.Lib.AdminNS.Views.CRUDPages.AdminNS.ListPage.ListPageAdmin"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProFind.Lib.AdminNS.Views.CRUDPages.AdminNS.ListPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:services="using:ProFind.Lib.Global.Services"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Padding="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*" />
            <ColumnDefinition Width="0.2*" />
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" Name="AdminsListView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="services:Admin">
                    <StackPanel Background="{StaticResource ButtonBackgroundThemeBrush}" CornerRadius="45">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="0.3*"/>
                                <ColumnDefinition Width="0.7*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Vertical">
                                <Image
                                    Width="100"
                                    Height="100"
                                    Source="{Binding PictureA}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Vertical" Padding="0 30">
                                <StackPanel>
                                    <TextBlock Height="25" Text="{Binding NameA}"  Style="{StaticResource ListTitleStyle}"/>
                                </StackPanel>
                                <StackPanel>

                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                FontWeight="Bold"
                                Text="Email:" />
                                    <TextBlock Margin="10 0 0 0" Height="31" Text="{Binding EmailA}" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                FontWeight="Bold"
                                Text="Phone number:" />
                                    <TextBlock Margin="10 0 0 0" Height="31" Text="{Binding TelA}" />
                                </StackPanel>
                                <StackPanel
                                     Grid.Column="1"
                                     HorizontalAlignment="Center"
                                      VerticalAlignment="Center">
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <Button Style="{StaticResource AccentButtonStyle}" Content="Add Admin" Grid.Column="1" VerticalAlignment="Top" Click="Button_Click_1" />
            <Button Style="{StaticResource AccentButtonStyle}" Content="Delete" Grid.Column="1" Margin="0,15,0,0" Click="Button_Click_3" Width="90" />
            <Button Style="{StaticResource AccentButtonStyle}" Content="Update" Grid.Column="1" Click="Button_Click_4" Width="90" Margin="0,15,0,0"/>
            <Button Style="{StaticResource AccentButtonStyle}" Visibility="Collapsed" Name="Generate_Report_btn" Content="Generate report" Grid.Column="1" Margin="0,15,0,0" VerticalAlignment="Top" Click="Generate_Report_btn_Click" />

        </StackPanel>
    </Grid>
</Page>
